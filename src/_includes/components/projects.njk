<section class="relative px-4 sm:px-10 py-12 overflow-hidden" id="projects">
	<!-- Subtle top divider -->
    <div class="max-w-4xl mx-auto mb-12">
        <div class="h-px bg-gray-200 w-full"></div>
    </div>

	<!-- Header -->
	<p class="text-center text-xs uppercase tracking-widest text-gray-400 mb-2">Projects</p>
	<p class="text-center text-3xl md:text-5xl font-bold leading-tight mb-2">Our latest projects</p>
 	<p class="text-center text-base text-gray-500 mb-10">A selection of recent work</p>

	<!-- Container -->
	<div class="flex flex-wrap justify-center gap-5 max-w-4xl mx-auto pb-5">
		{% for post in collections.project | limit(5) %}
			<!-- Individual entry -->
			<a href="{{ post.url }}" target="_blank" rel="noopener noreferrer">

				<div class="flex flex-col items-start space-y-2 w-64 min-h-64 bg-white border border-gray-200 rounded-xl p-6 transition-all duration-300 hover:-translate-y-1 hover:border-gray-300 hover:bg-gray-100">		
					<!-- Category Name -->	
					{% if post.data.tag %}
						<span class="self-start text-xs font-medium px-2 py-0.5 rounded-full bg-blue-100 border border-blue-400 text-blue-700">
							{{ post.data.tag }}
						</span>
					{% endif %}

					<!-- Entry Name -->	
					<p class="w-full text-base font-semibold tracking-tight text-gray-900">
						{{ post.data.title }}
					</p>

					<!-- Entry Description -->	
					<p class="w-full text-sm font-normal text-gray-500 leading-relaxed">
						{{ post.data.desc }}
					</p>

					<!-- Status pill -->
					{% if post.data.status == "Completed" %}
						<span class="self-start text-xs font-medium px-2 py-0.5 rounded-full bg-green-200 text-green-700 border border-green-400">Completed</span>
					{% else %}
						<span class="self-start text-xs font-medium px-2 py-0.5 rounded-full bg-yellow-200 text-yellow-700 border border-yellow-400">{{post.data.status}}</span>
					{% endif %}

					<!-- Release date -->	
					{% if post.data.date %}
						<div class="flex items-center gap-1.5 mt-auto pt-2">
							<span class="text-xs text-gray-400">{{ post.data.date | simplifyDate }}</span>
						</div>
					{% endif %}
				</div>
			</a>
		{% endfor %}
	</div>

	<!-- CTA -->
	<div class="flex justify-center">
		<a href="/projects/" class="bg-white inline-flex items-center gap-2 text-sm font-medium text-gray-800 border border-gray-300 rounded-lg px-5 py-2.5 transition-all duration-200 hover:border-gray-500">
			View all projects
			<span>→</span>
		</a>
	</div>
</section>